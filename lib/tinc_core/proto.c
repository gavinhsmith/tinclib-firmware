/* Builds the pinned protocol sources (external/tinclib-protocol) as part of
 * this library, so the ESP and native test builds both link them unchanged. */
#include "crc16.c"
#include "tinc_frame.c"
